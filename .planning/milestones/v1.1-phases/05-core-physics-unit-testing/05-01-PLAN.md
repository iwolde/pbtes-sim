---
phase: 05-core-physics-unit-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["tests/test_physics.py", "pytest.ini"]
autonomous: true
requirements: [PHYS-01, PHYS-03]
must_haves:
  truths:
    - "Developer can run unit tests to verify Molten Salt boundaries limits"
    - "Mathematical suppositions of properties calculation are verified"
  artifacts:
    - path: "tests/test_physics.py"
      provides: "Physics and property boundary tests"
      min_lines: 20
    - path: "pytest.ini"
      provides: "pytest configuration"
  key_links:
    - from: "tests/test_physics.py"
      to: "coreV5.py"
      via: "imported physics functions"
      pattern: "from coreV5 import"
---

<objective>
Establish the pytest framework and implement tests for core physical limits, specifically focusing on Molten Salt boundaries via CoolProp and verifying mathematical assumptions.

Purpose: Provide a deterministic foundation for physical validation (PHYS-01, PHYS-03).
Output: A suite of passing unit tests in `test_physics.py` and `pytest.ini` configuration.
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-physics-unit-testing/05-CONTEXT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Setup pytest and implement Molten Salt boundary tests</name>
  <files>tests/test_physics.py, pytest.ini</files>
  <read_first>
    - coreV5.py
    - errors_analysis.py
  </read_first>
  <behavior>
    - Test 1: Validate CoolProp fluid initialization limits for Molten Salt and Water
    - Test 2: Verify parameter out-of-bounds exceptions when temperatures exceed limits
  </behavior>
  <action>Create `pytest.ini` with basic configuration. In `tests/test_physics.py`, write tests using `pytest.mark.parametrize` to check physical boundaries. Specifically, instantiate CoolProp property lookups for Molten Salt (using the exact salt string used in `coreV5.py` or INCOMP::NaK as placeholder if absent) and verify they throw appropriate errors or return expected ranges when tested against limits. DO NOT use Hypothesis. Import any helper functions from `coreV5.py` directly.</action>
  <acceptance_criteria>
    - `pytest.ini` exists with `[pytest]` header
    - `tests/test_physics.py` contains `import pytest`
    - `tests/test_physics.py` contains `@pytest.mark.parametrize` for property boundaries
    - Running `pytest tests/test_physics.py` passes
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_physics.py -v</automated>
  </verify>
  <done>pytest is configured and boundary tests pass deterministically</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate logical and mathematical suppositions</name>
  <files>tests/test_physics.py</files>
  <read_first>
    - coreV5.py
    - errors_analysis.py
  </read_first>
  <behavior>
    - Test 1: Validate Stanton number calculation mathematically
    - Test 2: Validate basic energy balances and control logic
  </behavior>
  <action>Add tests to `tests/test_physics.py` that explicitly validate mathematical suppositions used in the system, such as Stanton number or heat transfer coefficients derived in `coreV5.py` (if available, otherwise basic energy math). Use known values to assert correct outputs. Verify the logic independently using `errors_analysis.py` functions if applicable.</action>
  <acceptance_criteria>
    - `tests/test_physics.py` contains tests for mathematical functions
    - Running `pytest tests/test_physics.py` passes without errors
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_physics.py -v</automated>
  </verify>
  <done>Math functions and logical constraints in the physics core are verified via tests</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing Environment | Tests run isolated from production deployment logic |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `tests/test_physics.py` | accept | Tests do not handle sensitive data; runs locally |
</threat_model>

<verification>
Run `pytest tests/test_physics.py` to ensure physical validation is correct.
</verification>

<success_criteria>
- `pytest` runner executes without failures
- Tests run deterministically without fuzzing
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-physics-unit-testing/05-01-SUMMARY.md`
</output>