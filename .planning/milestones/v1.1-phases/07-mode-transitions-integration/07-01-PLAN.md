---
phase: "07"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["tests/test_transitions.py"]
autonomous: true
requirements: ["TEST-02", "CONF-02"]
must_haves:
  truths:
    - "System can transition directly from any active mode to any other active mode"
    - "TES thermal state is preserved across mode transitions"
    - "Mass flow routes match expected topology for the new mode"
  artifacts:
    - path: "tests/test_transitions.py"
      provides: "NxN mode transition integration tests"
      exports: ["test_mode_transitions", "test_mass_flow_routing"]
  key_links:
    - from: "tests/test_transitions.py"
      to: "coreV5.py"
      via: "set_operation_mode and solve_network calls"
      pattern: "set_operation_mode.*profile"
---

<objective>
Build the NxN mode transition test framework and assert valid transient state handoffs.

Purpose: To ensure the system correctly routes flows and maintains TES history when changing states dynamically (D-01, D-02).
Output: Integration tests covering the transition matrix.
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-mode-transitions-integration/07-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transient Mode Transition Framework</name>
  <read_first>
    - tests/test_modes.py
    - coreV5.py
  </read_first>
  <files>tests/test_transitions.py</files>
  <behavior>
    - Test 1: Initialize system in Mode 1, solve, extract `tes.profile`, set mode to Mode 2 passing the profile, solve again, assert `tes.profile` is valid.
    - Test 2: Parametrize across all combinations of `[1, 2, 3, 4, 6]` to `[1, 2, 3, 4, 6]`.
  </behavior>
  <action>
    Create `tests/test_transitions.py` and implement the `test_mode_transitions` function. Use `pytest.mark.parametrize` to run through all mode pairs (e.g., `from_mode`, `to_mode`). In each test:
    1. Instantiate `SolarThermalSystem` and `Solver`.
    2. Solve the network in `from_mode`.
    3. Capture the `tes.profile`.
    4. Call `set_operation_mode` for `to_mode` passing the captured `profile` to ensure true transient state preservation (per D-02).
    5. Call `attempt_to_solve` or `solve_network` for `to_mode` and assert it converges.
  </action>
  <acceptance_criteria>
    - `tests/test_transitions.py` contains `def test_mode_transitions`
    - Uses `pytest.mark.parametrize` with pairs of modes
    - Calls `set_operation_mode` explicitly passing `profile=`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_transitions.py::test_mode_transitions -v</automated>
  </verify>
  <done>NxN mode combinations are tested while preserving TES thermal state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mass Flow Routing Verification</name>
  <read_first>
    - tests/test_transitions.py
    - coreV5.py
  </read_first>
  <files>tests/test_transitions.py</files>
  <behavior>
    - Test 1: When transitioning to Mode 3, assert `discharge_tes_pipe` exists and has positive mass flow.
    - Test 2: When transitioning to Mode 2, assert `charge_tes_pipe` has no flow or does not exist.
  </behavior>
  <action>
    Add `test_mass_flow_routing` to `tests/test_transitions.py`. For key transitions, explicitly assert that the correct pipes and connections exist and have the expected mass flows (per CONF-02).
    - In Mode 1/6: `charge_tes_pipe` mass flow > 0.
    - In Mode 3: `discharge_tes_pipe` mass flow > 0.
    - In Mode 2: Verify all flow goes to `process_hx` and TES lines are bypassed.
  </action>
  <acceptance_criteria>
    - `tests/test_transitions.py` contains `def test_mass_flow_routing`
    - Contains assertions for `charge_tes_pipe` and `discharge_tes_pipe` mass flows
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_transitions.py::test_mass_flow_routing -v</automated>
  </verify>
  <done>Mass flow paths are verifiably correct after transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System Integration | Internal simulation transitions, no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | Solver | accept | Mathematical failure modes handled via fallback logic, not a security risk |
</threat_model>

<verification>
Ensure all NxN transitions are executed without raising unexpected topology errors.
</verification>

<success_criteria>
Pytest verifies transient combinations and mass routing.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mode-transitions-integration/07-01-SUMMARY.md`
</output>