---
phase: 06-thermodynamic-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_physics.py, tests/test_modes.py]
autonomous: true
requirements: [PHYS-02, CONF-01]
must_haves:
  truths:
    - "Automated tests verify energy and mass balances in each solver iteration"
    - "System handles topological connections and safely fails on extreme bounds"
  artifacts:
    - path: "tests/test_physics.py"
      provides: "Independent Python analytical equations for energy balances"
      min_lines: 50
    - path: "tests/test_modes.py"
      provides: "Topological connection validations and extreme boundary testing"
      min_lines: 50
  key_links:
    - from: "tests/test_physics.py"
      to: "coreV5.py"
      via: "energy balance assertion"
      pattern: "assert abs"
---

<objective>
Implement thermodynamic consistency tests for energy balances (1st and 2nd law) and validate topological configurations with extreme bounds.

Purpose: To ensure the simulation logic holds true physically and the solver behaves robustly under anomalous conditions.
Output: Extended test suites in `tests/test_physics.py` and `tests/test_modes.py`
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-thermodynamic-consistency/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add independent Python analytical equations to verify energy balances (PHYS-02)</name>
  <files>tests/test_physics.py</files>
  <read_first>tests/test_physics.py</read_first>
  <behavior>
    - Test 1: Calculates independent energy balance based on states
    - Test 2: Asserts 1st law and 2nd law across components
  </behavior>
  <action>
Implement a new test function `test_energy_balances` in `tests/test_physics.py` per D-01. The test MUST initialize the `ThermalEnergyStorage` with valid parameters and verify that analytical energy balances (1st and 2nd law) hold across iterations. Use independent Python assertions (`assert abs(energy_in - energy_out) < tolerance`) rather than relying on TESPy's internal black box to verify these balances.
  </action>
  <verify>
    <automated>pytest tests/test_physics.py::test_energy_balances -v</automated>
  </verify>
  <acceptance_criteria>
    - File contains `def test_energy_balances`
    - Uses independent assertions to calculate and verify `energy_in` vs `energy_out`
    - Automated verify command exits 0
  </acceptance_criteria>
  <done>Independent energy balance tests implemented and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate parallel vs series topological connections and boundary extremes (CONF-01)</name>
  <files>tests/test_modes.py</files>
  <read_first>tests/test_modes.py</read_first>
  <behavior>
    - Test 1: Verifies the topology configuration (parallel/series) enforcement.
    - Test 2: Pushes the solver with non-physical bounds and verifies it fails safely.
  </behavior>
  <action>
Add a test function `test_topology_extreme_bounds` to `tests/test_modes.py`. Per D-03, this test MUST intentionally push the solver with extreme, non-physical boundary values (e.g. `PR_Q` or temperatures outside physical bounds, like a mass flow of 0 or infinity) and assert that it crashes loudly and safely (e.g. using `with pytest.raises(...)`).
Also, per D-04 and D-05, ensure the tests reflect that parallel and series configurations have distinct layouts and do NOT enforce identical energy outputs. Per D-02, rely on TESPy's internal mass balance errors during the network solve for mass flow consistency. 
  </action>
  <verify>
    <automated>pytest tests/test_modes.py::test_topology_extreme_bounds -v</automated>
  </verify>
  <acceptance_criteria>
    - File contains `def test_topology_extreme_bounds`
    - Contains `with pytest.raises` capturing solver failure for extreme boundary values
    - Automated verify command exits 0
  </acceptance_criteria>
  <done>Topological validation and extreme boundary tests implemented and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite | executes trusted local python code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test_physics.py | accept | Test files run in trusted environment |
</threat_model>

<verification>
Ensure all pytest tests run properly and energy balances are checked independently of TESPy logic.
</verification>

<success_criteria>
- `tests/test_physics.py` extended with energy balance checks.
- `tests/test_modes.py` extended with extreme boundaries test cases.
- All tests pass when running `pytest tests/`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-thermodynamic-consistency/06-01-SUMMARY.md`
</output>