---
phase: "07"
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: ["tests/test_transitions.py", "scripts/run_transition_matrix.py"]
autonomous: true
requirements: ["TEST-03"]
must_haves:
  truths:
    - "Solver gracefully falls back to Standby (Mode 4) on transition failure"
    - "Test report details unhandled exceptions and missing components"
  artifacts:
    - path: "scripts/run_transition_matrix.py"
      provides: "Exhaustive matrix runner and report generator"
      exports: ["generate_report"]
  key_links:
    - from: "tests/test_transitions.py"
      to: "coreV5.py"
      via: "attempt_to_solve fallback logic"
      pattern: "fallback|exception"
---

<objective>
Test convergence fallback behavior and generate transition exception report.

Purpose: To ensure robust failure handling (D-03) and provide a comprehensive report of any remaining missing components or unhandled exceptions across all paths.
Output: Fallback tests and a reporting script.
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-mode-transitions-integration/07-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convergence Fallback Tests</name>
  <read_first>
    - tests/test_transitions.py
    - coreV5.py
  </read_first>
  <files>tests/test_transitions.py</files>
  <behavior>
    - Test 1: Force a severe transition shock that violates thermodynamics (e.g., Mode 6 to Mode 3 with conflicting bounds).
    - Test 2: Assert that `attempt_to_solve` captures the error and cleanly falls back to Mode 4 (Standby) instead of bringing the test down.
  </behavior>
  <action>
    Add `test_convergence_fallback` in `tests/test_transitions.py`. Force an extreme boundary condition (like extreme PR_Q or invalid mass flow) during a mode switch, then verify that the solver invokes its fallback strategy to Mode 4 (per D-03).
    - If the solver does not currently fall back to Mode 4 correctly on exception, patch `coreV5.py`'s `attempt_to_solve` to set Mode 4 and try again upon persistent failures.
  </action>
  <acceptance_criteria>
    - `tests/test_transitions.py` contains `def test_convergence_fallback`
    - Asserts system mode evaluates to `4` after a failed transition
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_transitions.py::test_convergence_fallback -v</automated>
  </verify>
  <done>Convergence failures are safely caught and redirected to Mode 4.</done>
</task>

<task type="auto">
  <name>Task 2: Transition Report Generator</name>
  <read_first>
    - tests/test_transitions.py
  </read_first>
  <files>scripts/run_transition_matrix.py</files>
  <action>
    Create `scripts/run_transition_matrix.py` to programmatically run all NxN transitions and catch any unhandled exceptions, writing them to a Markdown or CSV report (`transition_report.md`).
    - The script should loop through all mode combinations, run the sequence, and log the success/failure state and exact error messages.
    - If a transition fails, it should be logged under "Unhandled Exceptions".
  </action>
  <acceptance_criteria>
    - `scripts/run_transition_matrix.py` creates a report file
    - Script handles and logs exceptions without crashing
  </acceptance_criteria>
  <verify>
    <automated>python scripts/run_transition_matrix.py</automated>
  </verify>
  <done>Transition script generates an outline of missing components and exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System Integration | Internal simulation transitions, no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Denial of Service | Solver | accept | Error reporting provides visibility into stability issues |
</threat_model>

<verification>
Ensure the report script runs and outputs the matrix results successfully.
</verification>

<success_criteria>
Tests verify fallback, and report is generated detailing transition stability.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mode-transitions-integration/07-02-SUMMARY.md`
</output>