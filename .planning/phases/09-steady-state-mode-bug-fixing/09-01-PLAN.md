---
phase: 09-steady-state-mode-bug-fixing
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - coreV5.py
  - test_run.log
autonomous: true
requirements:
  - BUG-01
user_setup: []
must_haves:
  truths:
    - Running `pytest` on the `tests/` directory initially shows failures, which are then resolved.
    - All previously failing tests now pass after fixes are applied.
  artifacts:
    - path: "coreV5.py"
      provides: "The corrected simulation logic."
    - path: "test_run.log"
      provides: "A log of the initial test failures."
  key_links:
    - from: "pytest"
      to: "coreV5.py"
      via: "Test execution that reveals bugs."
      pattern: "pytest tests/"
---

<objective>
Execute the test suite to identify all failing tests, analyze the root causes, and implement the necessary bug fixes in the core simulation engine. This plan establishes a baseline of passing tests before assertions are hardened.

**Purpose:** To resolve all existing, known bugs revealed by the test suite, fulfilling requirement BUG-01.
**Output:** A modified `coreV5.py` with fixes applied and a clean bill of health from the existing test suite.
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-steady-state-mode-bug-fixing/09-CONTEXT.md
@coreV5.py
@tests/test_modes.py
@tests/test_physics.py
@tests/test_topology.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Test Suite and Log Failures</name>
  <read_first>
    - tests/test_modes.py
    - tests/test_physics.py
    - tests/test_topology.py
  </read_first>
  <files>
    - test_run.log
  </files>
  <action>
  Execute the full `pytest` suite and redirect the output to `test_run.log`. This will serve as the definitive list of bugs to fix.
  
  Command:
  ```bash
  pytest > test_run.log 2>&1
  ```
  After execution, analyze `test_run.log` to identify every failing test case and the nature of the errors.
  </action>
  <verify>
    <automated>grep -E "failed|error" test_run.log</automated>
  </verify>
  <acceptance_criteria>
    - The file `test_run.log` exists.
    - `test_run.log` contains the full output of the pytest command.
    - A list of failing tests has been identified from the log.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Bug Fixes in Core Logic</name>
  <read_first>
    - coreV5.py
    - test_run.log
  </read_first>
  <files>
    - coreV5.py
  </files>
  <action>
  Based on the analysis of `test_run.log`, methodically address each failure. Per decision **D-01**, you may temporarily add `print()` statements or logging to `coreV5.py` to trace execution flow and variable states to pinpoint the root cause.
  
  Once the cause of a bug is identified, implement the necessary correction in the logic within `coreV5.py`. Repeat this process until all failures identified in `test_run.log` have been addressed. This task is complete when `pytest` runs with no failures.
  
  Per **D-03**, fix all bugs; do not defer any.
  </action>
  <verify>
    <automated>pytest</automated>
  </verify>
  <acceptance_criteria>
    - The `pytest` command exits with code 0.
    - The output of `pytest` shows that all tests passed.
    - Any temporary debugging statements have been removed from `coreV5.py` after fixes are confirmed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Simulation Core <=> Test Suite | The test suite provides inputs and asserts outputs from the simulation core. The core logic is complex and bugs can lead to incorrect physical modeling. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `coreV5.py` | mitigate | Per **D-01**, temporary logging/prints will be used for debugging. This task ensures they are removed before finalizing the plan to prevent leaking internal state. |
| T-09-02 | Tampering | `coreV5.py` | mitigate | The bug fixes must not introduce new physical inconsistencies. This is checked by running the full test suite. Plan 02 will further mitigate this by hardening assertions. |
</threat_model>

<verification>
The phase is verified when the bug fixes in `coreV5.py` result in the entire test suite passing.
</verification>

<success_criteria>
- All tests in `tests/` pass without errors.
- The `coreV5.py` file has been modified to fix the identified bugs.
- The `test_run.log` correctly captures the initial state of test failures.
</success_criteria>

<output>
After completion, create `.planning/phases/09-steady-state-mode-bug-fixing/09-01-SUMMARY.md`
</output>
