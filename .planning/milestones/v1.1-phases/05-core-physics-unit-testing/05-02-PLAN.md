---
phase: 05-core-physics-unit-testing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["tests/test_modes.py"]
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Each operating mode can be tested in complete isolation"
    - "Developer can observe explicit validation of control logic rules via unit tests"
  artifacts:
    - path: "tests/test_modes.py"
      provides: "Isolated network tests for modes 1, 2, 3, 4, 6"
  key_links:
    - from: "tests/test_modes.py"
      to: "coreV5.py"
      via: "SolarThermalSystem.create_network()"
      pattern: "create_network"
---

<objective>
Implement automated unit tests for single operation modes (1, 2, 3, 4, and 6) using `pytest`. Mode 5 must be explicitly skipped/deprecated.

Purpose: Guarantee that the equation systems for each supported mode are correctly stated and built in isolation (TEST-01).
Output: A suite of tests in `tests/test_modes.py` verifying network creation.
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-physics-unit-testing/05-CONTEXT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Setup isolated fixtures for network creation</name>
  <files>tests/test_modes.py</files>
  <read_first>
    - coreV5.py
  </read_first>
  <behavior>
    - Test 1: Initialize the Solver or SolarThermalSystem
    - Test 2: Network is built properly for a simple mode without cross-contamination
  </behavior>
  <action>Create `tests/test_modes.py`. Define `pytest` fixtures that instantiate the `SolarThermalSystem` or `Solver` from `coreV5.py` with basic parameters. Write tests ensuring the fixtures provide a fresh, isolated state for each test run.</action>
  <acceptance_criteria>
    - `tests/test_modes.py` contains `@pytest.fixture` for the system class
    - File imports the main classes from `coreV5.py`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_modes.py -v -k "not parametrization"</automated>
  </verify>
  <done>Isolated fixtures are created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test network creation for modes 1, 2, 3, 4, and 6</name>
  <files>tests/test_modes.py</files>
  <read_first>
    - coreV5.py
  </read_first>
  <behavior>
    - Test 1: create_network(mode=1) successfully defines the network
    - Test 2: create_network(mode=2) successfully defines the network
    - Test 3: create_network(mode=3) successfully defines the network
    - Test 4: create_network(mode=4) successfully defines the network
    - Test 5: create_network(mode=6) successfully defines the network
  </behavior>
  <action>Implement `pytest.mark.parametrize` over the allowed modes `[1, 2, 3, 4, 6]`. For each mode, assert that `create_network` (or the equivalent initialization) completes successfully, resulting in a correctly instantiated TESPy network. Explicitly omit/deprecate Mode 5. Verify the network has the expected number of components or connections based on the mode.</action>
  <acceptance_criteria>
    - `tests/test_modes.py` uses `@pytest.mark.parametrize` for modes `1, 2, 3, 4, 6`
    - Mode 5 is noticeably absent or expressly skipped
    - Running `pytest tests/test_modes.py` passes for all parameterized modes
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_modes.py -v</automated>
  </verify>
  <done>All active modes can be successfully built in isolation</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing Environment | Tests run isolated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Denial of Service | `tests/test_modes.py` | mitigate | Test execution time limited by simple network instantiation rather than full convergence solves |
</threat_model>

<verification>
Run `pytest tests/test_modes.py`
</verification>

<success_criteria>
- Mode 5 is avoided.
- Modes 1, 2, 3, 4, 6 can be created in complete isolation.
- `pytest` executes without failures.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-physics-unit-testing/05-02-SUMMARY.md`
</output>