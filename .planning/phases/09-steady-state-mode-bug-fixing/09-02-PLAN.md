---
phase: 09-steady-state-mode-bug-fixing
plan: 2
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - tests/test_modes.py
  - tests/test_physics.py
autonomous: true
requirements:
  - BUG-02
user_setup: []
must_haves:
  truths:
    - The automated test suite executes completely with zero failures.
    - Assertions in tests are measurably stricter than at the start of the phase.
    - All operation modes (1-6) are verified to produce correct thermodynamic states by the hardened tests.
  artifacts:
    - path: "tests/test_modes.py"
      provides: "Hardened test cases with stricter validation for all 6 operation modes."
      contains: "abs=1e-2"
    - path: "tests/test_physics.py"
      provides: "Hardened test cases with stricter physical boundary checks."
      contains: "abs=1e-3"
  key_links:
    - from: "tests/test_modes.py"
      to: "coreV5.py"
      via: "Stricter assertions that confirm bug fixes are robust."
      pattern: "assert"
---

<objective>
With a baseline of passing tests established, this plan hardens the test suite by tightening numerical tolerances and adding new physical assertions. This ensures that the system doesn't just pass tests, but that it produces verifiably correct results under stricter scrutiny.

**Purpose:** To fulfill requirement BUG-02 by ensuring all modes yield correct results, verified by a more robust and precise test suite.
**Output:** Updated test files (`test_modes.py`, `test_physics.py`) with hardened assertions and a final, successful test run.
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-steady-state-mode-bug-fixing/09-CONTEXT.md
@.planning/phases/09-steady-state-mode-bug-fixing/09-01-SUMMARY.md
@tests/test_modes.py
@tests/test_physics.py
@coreV5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Assertions in `test_modes.py`</name>
  <read_first>
    - tests/test_modes.py
  </read_first>
  <files>
    - tests/test_modes.py
  </files>
  <action>
  Per decision **D-02**, review all `assert` statements in `tests/test_modes.py`.
  
  1.  Identify assertions using `pytest.approx` or similar fuzzy comparisons.
  2.  Tighten the tolerance. For example, change `pytest.approx(expected, abs=1e-1)` to `pytest.approx(expected, abs=1e-2)`.
  3.  Add new assertions that check for physical correctness in each mode, such as temperature drops across coolers and rises across heaters.
  
  After modifying the file, run the test suite to ensure the fixes from Plan 01 are robust enough to pass these stricter checks.
  </action>
  <verify>
    <automated>pytest tests/test_modes.py</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 `pytest.approx` tolerances in `tests/test_modes.py` have been made smaller (e.g., abs value decreased).
    - At least 2 new `assert` statements checking for physical laws (e.g., `T_out < T_in` for a cooler) have been added.
    - `pytest tests/test_modes.py` exits with code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Harden Assertions in `test_physics.py`</name>
  <read_first>
    - tests/test_physics.py
  </read_first>
  <files>
    - tests/test_physics.py
  </files>
  <action>
  Per decision **D-02**, review all `assert` statements in `tests/test_physics.py`.
  
  1.  Tighten the tolerance of numerical comparisons. For example, change `abs=1e-2` to `abs=1e-3`.
  2.  Add new assertions to check for other physical invariants or boundary conditions that are not currently tested.
  
  After modifying the file, run the full test suite to ensure all tests still pass.
  </action>
  <verify>
    <automated>pytest</automated>
  </verify>
  <acceptance_criteria>
    - At least 2 numerical tolerances in `tests/test_physics.py` have been made stricter.
    - At least 1 new physical assertion has been added to the file.
    - The full `pytest` command exits with code 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Simulation Core <=> Test Suite | This plan hardens the assertions, making the test suite a more reliable verifier of the simulation core's correctness. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | `tests/*.py` | mitigate | Hardening assertions ensures that "passing the test" is a meaningful statement about physical correctness, reducing the risk of accepting incorrect results. This makes future undetected tampering with the core logic less likely. |
| T-09-04 | Repudiation | Test Results | mitigate | By making tests stricter, we have a higher degree of confidence that a "pass" result is not accidental, and can more strongly repudiate claims that the model is incorrect for the tested scenarios. |
</threat_model>


<verification>
The phase is verified when the full test suite passes with the newly hardened assertions.
</verification>

<success_criteria>
- All tests in `tests/` pass with stricter, more precise assertions.
- `tests/test_modes.py` and `tests/test_physics.py` have been updated with tightened numerical tolerances and new assertions.
- The final `pytest` run shows 100% of tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/09-steady-state-mode-bug-fixing/09-02-SUMMARY.md`
</output>
