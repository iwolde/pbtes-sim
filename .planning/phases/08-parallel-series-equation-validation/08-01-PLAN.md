---
phase: 08-parallel-series-equation-validation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/test_configurations.py"
  - "tests/conftest.py"
  - "pytest.ini"
autonomous: true
requirements: ["CONF-01", "CONF-02"]
user_setup: []
must_haves:
  truths:
    - "An automated test can be run to verify the Parallel configuration's equations."
    - "An automated test can be run to verify the Series configuration's equations."
    - "The tests explicitly check for and prevent cross-contamination between configurations."
  artifacts:
    - path: "tests/test_configurations.py"
      provides: "Pytest tests for Parallel and Series configurations."
    - path: "tests/conftest.py"
      provides: "Fixtures for test configurations (e.g., tes_params)."
    - path: "pytest.ini"
      provides: "Configuration for the pytest runner."
  key_links:
    - from: "tests/test_configurations.py"
      to: "coreV5_5.py"
      via: "import Solver, SolarThermalSystem"
      pattern: "from coreV5_5 import"
---

<objective>
To establish a testing framework and create specific, automated validation tests for the Parallel and Series system configurations, ensuring their equations are strictly separated and correct.

**Purpose:** This plan addresses the 0% test coverage identified in `TESTING.md` and directly implements the core requirements of Phase 8. It provides a repeatable, automated way to verify the thermodynamic correctness of the two primary system topologies.
**Output:** A new test suite for configuration validation that can be run with `pytest`.
</objective>

<execution_context>
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/iwold/OneDrive - Universidad Católica de Chile/Postdoc/Galvanizing solar PBTES/codigos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-parallel-series-equation-validation/08-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Pytest Scaffolding and Fixtures</name>
  <files>
    - "tests/conftest.py"
    - "pytest.ini"
  </files>
  <behavior>
    - `pytest.ini` should configure the python_files and python_functions prefixes.
    - `conftest.py` should provide fixtures for common parameters like `tes_params`, `component_params`, etc., to be reused by tests.
  </behavior>
  <action>
    1. Create a `pytest.ini` file in the project root. Configure it to recognize `tests/test_*.py` as test files.
    2. Create `tests/conftest.py`.
    3. Based on the configuration patterns in `CONVENTIONS.md`, create pytest fixtures within `conftest.py` for `tes_params`, `component_params`, and `conexion_params`. These fixtures will provide standardized inputs for the validation tests.
  </action>
  <verify>
    <automated>pytest --collect-only</automated>
  </verify>
  <done>
    - Pytest is configured and recognizes the test directory.
    - Fixtures for simulation parameters are available for injection into tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Parallel Configuration Validation Test</name>
  <files>
    - "tests/test_configurations.py"
  </files>
  <behavior>
    - The test must instantiate a new `Solver` and `SolarThermalSystem` (per D-03).
    - The test should use a specific input set designed to trigger Series-only components, which should not be present in the Parallel topology (per D-01).
    - The test must calculate expected thermodynamic outputs analytically and compare them to the simulation result (per D-02).
  </behavior>
  <action>
    1. Create a new test file: `tests/test_configurations.py`.
    2. Import `Solver`, `SolarThermalSystem`, and the parameter fixtures.
    3. Create a test function `test_parallel_configuration_validation`.
    4. **Inside the test (per D-03):** Create a fresh instance of the `Solver` using the 'parallel' configuration flag.
    5. **(Per D-01):** Provide it with input parameters (e.g., mass flow splits) that are only relevant to the Series configuration. The test will assert that these parameters do not affect the outcome, or that components specific to the series configuration are not present in the network's component list.
    6. **(Per D-02):** Run a single step of the simulation. Calculate the expected outlet temperatures and energy balance based on a simplified analytical model for the parallel circuit.
    7. Use `assert` statements with a tolerance (e.g., `pytest.approx`) to compare the simulation results against the analytically calculated values.
  </action>
  <verify>
    <automated>pytest tests/test_configurations.py::test_parallel_configuration_validation</automated>
  </verify>
  <done>
    - An automated test successfully validates the parallel configuration.
    - The test confirms that series-specific logic does not contaminate the parallel model.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Series Configuration Validation Test</name>
  <files>
    - "tests/test_configurations.py"
  </files>
  <behavior>
    - The test must instantiate a new `Solver` and `SolarThermalSystem` (per D-03).
    - The test should use a specific input set designed to fail if parallel-specific logic is present (per D-01).
    - The test must calculate expected thermodynamic outputs analytically and compare them to the simulation result (per D-02).
  </behavior>
  <action>
    1. Add a new test function `test_series_configuration_validation` to `tests/test_configurations.py`.
    2. **Inside the test (per D-03):** Create a fresh instance of the `Solver` using the 'series' configuration flag.
    3. **(Per D-01):** Provide it with input parameters or check for components that are only relevant to the Parallel configuration. The test will assert that these do not affect the outcome or are not present.
    4. **(Per D-02):** Run a single step of the simulation. Calculate the expected outlet temperatures and energy balance based on a simplified analytical model for the series circuit.
    5. Use `assert` statements with `pytest.approx` to compare the simulation results against the analytically calculated values.
  </action>
  <verify>
    <automated>pytest tests/test_configurations.py::test_series_configuration_validation</automated>
  </verify>
  <done>
    - An automated test successfully validates the series configuration.
    - The test confirms that parallel-specific logic does not contaminate the series model.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Code -> Core Logic | Untrusted test inputs (though controlled) are passed to the simulation engine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `tests/test_configurations.py` | Mitigate | The tests themselves are the mitigation. By calculating expected values independently (D-02) and asserting correctness, they prevent unintentional tampering (i.e., bugs) with the core physics equations from going undetected. |
| T-08-02 | Information Disclosure | Test outputs | Accept | Test outputs are just numbers (temperatures, pressures) and contain no sensitive information. The risk of disclosure is minimal and acceptable. |
</threat_model>

<verification>
- Run `pytest` in the root directory.
- All tests in `tests/test_configurations.py` must pass.
- The test report should show 3 collected items passing.
</verification>

<success_criteria>
- The project has a foundational, working `pytest` test suite.
- Both Parallel and Series configurations are validated by automated, repeatable tests.
- All decisions (D-01, D-02, D-03) from `08-CONTEXT.md` are demonstrably implemented in the test code.
</success_criteria>

<output>
After completion, create `.planning/phases/08-parallel-series-equation-validation/08-01-SUMMARY.md`
</output>
